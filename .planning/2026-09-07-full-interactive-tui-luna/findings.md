Configured approved endpoint: https://api.12th.day/v1 (openai-response). Existing user default is a different model; isolated config is required. Installed tmux/Bun/hya binaries available.

Runtime findings: advertised builtin skill agent-bundle-authoring fails both short and harness:skill identity; a native project luna-qa skill succeeds. LSP fails on a real TypeScript fixture. Keyboard single/double Escape during observed streaming sends no HTTP abort request in the audited TUI session; direct API abort succeeds. Heap diagnostic reports undefined. No fixes made.
