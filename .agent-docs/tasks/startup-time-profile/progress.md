# Progress

- Mapped owned-mode path: hya → hya-ts → backend listen → bun src/main.tsx → marks.
- Release binaries built; `startup-bench` + custom `profile-e2e.mjs` (F0 + realistic).
- Isolated Bun load vs `dist/` prebundle; timed `/tui/bootstrap` vs `/session` on 84MB DB.
- Wrote `findings.md` with overlap + lazy-load ledger.
- Skipped git commit (user rule: only when explicitly requested).
