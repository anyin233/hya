//! `yaca` — umbrella binary (interactive TUI + embedded server; `serve`; `-p`).
//! Phase 0 scaffold: subcommands land in later phases.
fn main() {
    eprintln!("yaca: scaffold (Phase 0) — not yet implemented");
    std::process::exit(0);
}
