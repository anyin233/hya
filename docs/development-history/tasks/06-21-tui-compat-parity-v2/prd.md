# TUI compat 1:1 parity v2

## Goal

Optimize yaca TUI to true 1:1 feature + visual parity with compat, building on the tui/compat-parity (W0-W7) branch in worktree tui-parity-v2. Driven by a fresh two-agent distillation of compat's @opentui/solid TUI.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
