# Documentation audit findings

## Scope and method

- Task: repository-wide synchronization planning only.
- Immutable history: `docs/changes/CHANGELOG_*.md` is excluded from edit scope.
- Evidence sources loaded so far: root `README.md`, `DESIGN.md`, `CHANGELOG.md`, `Cargo.toml`, `CONTEXT.md`, `docs/README.md`, Trellis package/spec indexes.
- CodeGraph is unavailable because this repository has no `.codegraph/` index; use narrow source reads/searches and command help instead.

## Confirmed baseline

- Workspace version is `0.36.7`, edition 2024, Rust `1.91`, license `MIT OR Apache-2.0` (`Cargo.toml:8-12`).
- Current changelog heading is `0.36.7` and records Workflow Stage model fallback routing with request-local assignment and bounded replay metadata (`CHANGELOG.md:1-10`).
- Root README describes event-sourced Rust runtime, normalized OpenAI-compatible/Anthropic/Google providers, permission plane, TUI/headless/HTTP-SSE surfaces, offline `dev` provider, source-only install, adjacent `hya`/`hya-ts`/`hya-backend` binaries, WorkflowBundle/AgentBundle packaging, and docs map (`README.md:8-124`).
- Docs index describes user and architecture reading paths and source entrypoints, including TypeScript TUI ownership (`docs/README.md:1-123`).
- Project glossary states current Agent/AgentBundle/Workflow/WorkflowBundle, tool-plane, TUI, roster/sidebar, and lifecycle terminology (`CONTEXT.md:107-299`; remainder to inspect).

## Pending audit slices

- Root and metadata audit: delegated scout.
- Architecture/API/provider/workflow audit: delegated scout.
- TUI/frontend audit: delegated scout.
- Wiki/package README/testing audit: delegated scout.
- Source, CLI help, version metadata, routes, release packaging, and test matrix remain to be inspected directly and merged into this file before final artifacts.
- Owned Markdown relative-link scan (excluding dependency/vendor/target trees and immutable historical changelogs) found 157 files and zero broken relative links. Verification: Python `Path`/Markdown-link scan, 2026-08-31.
- Current source facts: `hya` is a 26-line Unix exec shim to adjacent `hya-ts` (`crates/hya/src/main.rs:1-26`); `hya-ts` parses launcher flags, starts/attaches `hya-backend`, and hands terminal control to Bun (`crates/hya-ts/src/main.rs:33-137`); the TUI package requires `--url` (`packages/hya-tui-ts/README.md:1-10`).
- Native server routes are `/sessions`, `/sessions/:id/{prompt,command,shell,workflow}`, `/sessions/:id/{events,stream}` (`crates/hya-server/src/lib.rs:49-75`). Compatibility routes are merged from the module router (`crates/hya-server/src/compat.rs:95-126`).
- `hya-backend workflow` currently exposes `list`, `info`, `use`, `run`, and `state`; route assignments contain preferred model, reasoning, and ordered fallback candidates (`crates/hya-backend/src/workflow_cmd.rs:19-76`; `crates/hya-workflow/src/model.rs:86-176`).
- Provider resolution is first-match; pre-stream retry/failover only applies before an established stream, and unsupported reasoning is stripped before dispatch (`crates/hya-provider/src/router.rs:44-145`).
- Install/release package three Rust binaries plus the prepared TUI and Compat adapter (`install.sh:22-28,75-83,205-223`; `.github/workflows/release.yml:92-130`). Release verifies `README.md`, current changelog/version, help output, runtime prune, adapter probe, and Argus example (`.github/workflows/release.yml:56-90,135-250`).
- CI enforces TypeScript build/typecheck, focused Track T tests, matrix-check, Rust checks/tests, Track P E2E, and no-HTTP verification; full Bun test is non-gating (`.github/workflows/ci.yml:35-102`).
- Confirmed documentation risk: `docs/superpowers/plans/2026-06-29-install-script.md` contains the old 0.28.3→0.28.4 plan and old two-binary packaging claims (version inventory output). It is a historical plan, not a current guide; preservation versus annotation/removal is a scope decision.
- Wiki index is present at `.autors/hya/wiki/INDEX.md`; wiki README points to it (`.autors/hya/wiki/README.md:3-7`, `.autors/hya/wiki/INDEX.md:1-7`).
- Wiki Rust-toolchain page states machine-specific rustup state and says the pin was not done “as of 0.35.2” (`.autors/hya/wiki/pages/environment/rust-toolchain.md:8-29`); this is time-sensitive operational evidence and should not be treated as a current product guarantee without a fresh environment check.
## Confirmed stale claims and omissions

### Root and metadata

- `README.md:21-24` says hya is not published as a prebuilt binary. The public repository release API reports a published `v0.35.1` Linux archive (`hya-0.35.1-x86_64-unknown-linux-gnu.tar.gz` and `SHA256SUMS`; checked 2026-08-31 at `https://api.github.com/repos/anyin233/hya/releases/latest`). The source workspace is now `0.36.7` (`Cargo.toml:8-12`, `README.md:21`), so status wording must distinguish the latest published release from unreleased `main`; crates.io index lookup for `hya` returned 404.
- `README.md:40-42` lists the three binaries and prepared TUI but omits the shipped Compat adapter. `install.sh:22-27,200-214` and `.github/workflows/release.yml:109-125,244-247` install/package `lib/hya/compat-adapter` with production dependencies.
- `README.md:8-13` describes the product as written in Rust without stating the ownership split. `crates/hya/src/main.rs:1-26` is only a Unix exec shim, `crates/hya-ts/src/main.rs:1-95` supervises the backend and Bun, and `packages/hya-tui-ts/package.json:2-8` owns the private Solid/OpenTUI frontend. Rewrite the claim to name the Rust runtime/binaries and TypeScript frontend chain.
- `docs/README.md:104-123` labels `crates/hya/src/main.rs` the canonical frontend entrypoint and omits `hya-workflow`, the Workflow executor/control, and bundle compiler source entries. The source roles are `crates/hya/src/main.rs:1-26` (shim), `crates/hya-workflow/src/lib.rs:1-20` (compiler), `crates/hya-core/src/workflow/mod.rs:1-58` (executor), `crates/hya-app/src/workflow_control.rs` (control), and `crates/hya-bundle/src/lib.rs` (package model).
- `DESIGN.md:125-131` says the prompt is disabled while running. The current session route only disables it for pending permission/question overlays (`packages/hya-tui-ts/src/upstream/routes/session/index.tsx:252-265,1660-1689`); submitted prompts can remain queued and render `QUEUED` (`:1766-1833`). The design state must separate editable/queued running behavior, overlay-disabled behavior, and observation-focused absence.
- `DESIGN.md:166-168` limits borders to modal overlays, while its own user-message rule already permits semantic left borders (`DESIGN.md:114-117`) and source renders left borders for user messages and prompt (`packages/hya-tui-ts/src/upstream/routes/session/index.tsx:1773-1783`; `packages/hya-tui-ts/src/upstream/component/prompt/index.tsx:1321-1330`) plus a compaction top separator (`:1837-1844`). Rewrite the rule to prohibit decorative framed cards but allow semantic accent/separator/split borders.
- `DESIGN.md:50` bans all raw color literals in render code, but retained source has deliberate fallback/data literals in `packages/hya-tui-ts/src/upstream/component/error-component.tsx:17-21`, `.../logo-art.data.ts:4-6`, and `.../feature-plugins/system/which-key.tsx:102-110`. Decide whether to enforce code cleanup or document narrow upstream bootstrap/fallback/data exceptions.
- `CONTEXT.md:120-154` defines categories as if they come from agent files. Current `CategoryRegistry` entries are loaded from the config `categories:` block (`crates/hya-core/src/category.rs:141-144`; `crates/hya-app/src/config.rs:114-120,1420-1430`); Agent/Bundle model policies reference categories but do not author the registry. Update the glossary.
- `CONTEXT.md:235-241` defines Stage without its optional worker/verifier model assignments, and the glossary has no Workflow model-route or route-outcome terms. Current contracts are `crates/hya-workflow/src/model.rs:86-193,240-249`, `crates/hya-core/src/workflow/run.rs:287-366`, and `crates/hya-proto/src/event.rs:178-205`.
- `CHANGELOG.md:1-10` is factually current but its first bullet can imply every Stage uses a route. Clarify that ordered routes apply only when a Stage or loop verifier declares an explicit model assignment.
- License wording needs an owner decision: root Rust metadata and `README.md:21-22,126-128` claim dual MIT/Apache licensing, while `packages/hya-tui-ts/package.json:4-8` and its `LICENSE` are MIT/upstream, the adapter has no license file (`crates/hya-plugin-compat/adapter/package.json:1-20`), and release packaging copies the TUI license but no root license texts (`.github/workflows/release.yml:114-123`).
- Version ownership needs an explicit policy. Rust workspace, TUI package, and built binaries are `0.36.7` (`Cargo.toml:8-12`, `packages/hya-tui-ts/package.json:3-7`, version smoke); adapter metadata and `src/main.ts` remain `0.0.0` (`crates/hya-plugin-compat/adapter/package.json:2-5`, `src/main.ts:3-8`), and TUI `HYA_VERSION` defaults to `local` (`packages/hya-tui-ts/src/hya/platform.ts:51-52`). Decide whether private component versions stay independent or release tooling synchronizes them.

### Workflow, component map, and event model

- `docs/project-structure.md:31-54` omits the `hya-workflow` crate and describes `hya-bundle` only as AgentBundle support. It also omits Workflow execution from `hya-core`, WorkflowControl from `hya-app`, and Workflow tests from the crate map. Current ownership is visible in `Cargo.toml:16-27`, `crates/hya-workflow/src/lib.rs`, `crates/hya-core/src/workflow/`, `crates/hya-app/src/workflow_control.rs`, `crates/hya-bundle/src/lib.rs`, and `crates/hya-workflow/tests/`.
- `docs/project-structure.md:98-136` lists builtin tools but lacks the canonical `workflow` row and `workflow_plane` module; `crates/hya-tool/src/tool.rs:329-361` registers both `WorkflowTool` and `AnnounceTool`.
- `docs/project-structure.md:232-252` gives only the native six-route summary and says `hya-client` is a small wrapper, but does not distinguish `hya-client`'s native three-call client (`crates/hya-client/src/lib.rs:85-149`) from the frozen Compat `hya-sdk` Client, `hya-sdk::native` Bun JSONL bridge, and `hya-native` in-process Axum transport (`crates/hya-sdk/src/client.rs:25-85,290-350`, `crates/hya-sdk/src/native.rs:1-155`, `crates/hya-native/src/transport.rs:1-93`). Add cross-links while keeping `server-client.md` canonical for route details.
- `docs/architecture/overview.md:28-37` component boundaries omit `hya-workflow`, `hya-app`, `hya-bundle`, and `hya-native`, and `:39-58` does not show WorkflowControl/compiled Workflow execution. Align the overview with the current project map without duplicating the full route table.
- `docs/architecture/event-model.md:68-200` claims a full 45-variant Event catalog but omits all seven `Workflow*` variants, `SessionForked`, `ContextCompacted`, and `ContextEvicted`; current `Event` has 54 variants (`crates/hya-proto/src/event.rs:32-718`, count by enum declaration). Add the current Workflow and context lifecycle sections, or change the heading to a deliberately scoped catalog.
- `docs/architecture/event-model.md:373-392` documents generic projection folding but does not state that `ContextCompacted` is a durable replay checkpoint/baseline marker. Current persistence/rebuild is in `crates/hya-core/src/engine/turn.rs:642-710,948-989`, event definition in `crates/hya-proto/src/event.rs:642-717`, and tests in `crates/hya-core/tests/compaction.rs:540-615`.
- `docs/architecture/runtime.md:158-164` lists the Workflow durable event sequence but omits `WorkflowStageRouteOutcome`, which is now emitted for explicit route stream groups (`crates/hya-proto/src/event.rs:178-205`). Include the bounded outcome and replay semantics.

### Tool inventory and Compat parity

- `docs/architecture/tools-and-permissions.md:12-23,25-54` says `ToolRegistry::builtins()` has 26 canonical schemas, omits `workflow` and `announce`, and links an old source range (`tool.rs#L311-L347`). Current registration is 28 canonical schemas at `crates/hya-tool/src/tool.rs:329-361`: 21 array entries including `workflow`/`announce`, `shell`, `bash`, and five canonical names installed with hidden aliases. `docs/testing/agent-matrix.md:68-75` is the current 28-name/count reference and must remain consistent.
- `docs/architecture/agent-tool-surface.md:22-35,176-180,225-235` likewise says 26 and omits `workflow`, while retaining old `tool.rs#L313-L347` anchors. Update count, inventory, and links to the same 28-name source/test evidence.
- `docs/compat-parity.md:84` says 26 canonical tools and omits `workflow` and `announce`; update the row to 28 while preserving the five hidden aliases and the actual Track P covered/not-covered split.
- `docs/compat-parity.md:88` says automatic compaction is request-local and lacks durable checkpoints/baseline rebuild. Current hya compaction persists and replays `ContextCompacted` (same source/test anchors above). Keep only actual Compat gaps such as Compat compaction parts and message-summary diffs in the row and matching `:109` gap entry.
- `docs/compat-parity.md:90` lists plugin tools/hooks but omits native plugin Skill contributions. `PluginContribution::Skill` and Bundle Skill contributions are defined at `crates/hya-bundle/src/lib.rs:35-47,203-343`; app registration is `crates/hya-app/src/plugins.rs:24-40,254-286`, covered by `:342-407` tests.
- `docs/plugin-protocol.md:62-68` broadly says bundle contributions coexist without naming Workflow and Skill; this is an underspecified contract, not a contradictory claim. Cross-link the explicit contribution inventory in `docs/agent-bundle-authoring.md` and `docs/workflows.md`.

### TypeScript TUI, packaging, and tests

- `docs/architecture/tui.md:318-332` claims 11 declared/10 default builtin plugins and lists `internal:home-footer` first. Current `createBuiltinPlugins()` has 12 declared, with `internal:sidebar-workflow` first and `which-key` disabled, so 11 start by default (`packages/hya-tui-ts/src/upstream/feature-plugins/builtins.ts:21-34`; `packages/hya-tui-ts/test/workflow-sidebar.test.ts:29-30,126-129`). Update count, order, and list.
- `docs/tui-reference.md:77-91` and `docs/adr/0006-tui-session-reset-and-subagent-visibility.md:25-42` list sidebar sections without Workflow. The synchronized `sidebar-workflow.tsx` plugin is registered first and renders Workflow state (`packages/hya-tui-ts/src/upstream/feature-plugins/sidebar/workflow.tsx:7-19,46-69,73-89`); `docs/workflows.md:272-279` describes the current behavior. Keep the no-roster statement but add Workflow.
- `packages/hya-tui-ts/test/README.md:1-10` says there are eleven Bun test files and its suite tables stop before the Workflow suites. There are 14 current `*.test.ts` files, including `workflow-presentation.test.ts`, `workflow-pty.test.ts`, and `workflow-sidebar.test.ts`; update count and coverage tables using `packages/hya-tui-ts/test/*.test.ts` and those three tests as evidence.
- `docs/testing/agent-matrix.md:52-64,93-105` omits machine-registry `T2.14` (`Workflow stage model routing and replay`) even though `crates/hya-e2e/matrix.toml:283-289` registers it at `crates/hya-e2e/tests/p19_workflow_model_routing.rs`. Add the row and exact replay/route oracle.
- `docs/testing/README.md:57-66` calls 85.56% the current workspace figure, but `docs/testing/coverage-baseline.md:9-12,28-34` records that value for 2026-08-05 commit `1a7db256`; current source has since added Workflow routing. Reword the index as a dated baseline or regenerate only with explicit scope approval. Keep dated baseline tables (`coverage-baseline.md:78-90,133-155,174-185`) as historical evidence rather than rewriting their 18-binary/27-scenario measurements; current registry is 19 binaries/28 scenarios.
- `docs/getting-started.md:37-39,63-75` describes installed TUI runtime but omits the installed Compat adapter and its verification. `docs/architecture/tui.md:448-462` similarly lists only `lib/hya/hya-tui-ts`; current installer/release layout also includes `lib/hya/compat-adapter` (`install.sh:22-27,208-223`; `.github/workflows/release.yml:109-128,244-247`).
- `.autors/hya/wiki/pages/environment/rust-toolchain.md:8-29` contains machine-specific rustup state, says no pin “as of 0.35.2,” and prescribes a toolchain override. Current task environment does not expose `rustc` in PATH, so the machine claim is not freshly verifiable; decide whether to preserve as historical operational evidence, refresh from a known environment, or move it out of current guidance. Do not rewrite the historical install plan `docs/superpowers/plans/2026-06-29-install-script.md`.

## Ambiguous claims requiring explicit scope decisions

- `docs/workflows.md:188-192` calls every Workflow model id “fully-qualified,” but compiler validation only rejects empty/overlong/`#`-suffixed ids and does not require provider qualification (`crates/hya-workflow/src/compiler.rs:395-434`). Treat as convention or make the compiler enforce it; do not silently change either contract.
- Decide whether `compat-parity.md` remains Compat-only (with a Workflow cross-link) or adds native-only Workflow rows; avoid presenting native extensions as Compat parity.
- Decide whether architecture docs inventory every Compat route or defer exhaustive route discovery to generated `/doc`/`/openapi.json`; retain one canonical route table in `docs/architecture/server-client.md`.

### Additional testing, package, and operational drift

- `crates/hya-plugin-compat/README.md:1-9` says the Rust crate pins the Compat package dependencies, but `crates/hya-plugin-compat/Cargo.toml:1-9` has no dependency pins. The actual `@opencode-ai/plugin`/SDK `1.17.9` pins are in `crates/hya-plugin-compat/adapter/package.json:10-15` and match `packages/hya-tui-ts/package.json:14-19`; rewrite ownership.
- `docs/examples/self-update/README.md:3-10` calls the owner-authorized re-apply optional and omits the required discard step. `run-demo.sh:41-63` always stages, reports, discards sequence 1, then activates; same-sequence re-stage is rejected by the immutable stage rule (`crates/hya-updater/src/stage.rs:26-49`). Clarify the exact walkthrough and distinguish feature introduction `0.34.13` from the checked-out updater version (`0.36.7`).
- `.autors/hya/wiki/pages/environment/rust-toolchain.md:28-29` says the missing toolchain pin was true “as of 0.35.2”; the workspace is now `0.36.7` (`Cargo.toml:8-12`), so the qualifier is stale even though no root pin remains. The wiki's machine-specific override commands (`:8-26`) also conflict with plain cargo commands in `docs/testing/README.md:27-30`, `crates/hya-e2e/README.md:14-17`, `docs/testing/process-e2e.md:146-153`, and `docs/examples/self-update/run-demo.sh:7-10,27-39` on hosts whose default toolchain is 1.90. Keep the wiki as a separate Agent-authored channel and make the pin/override policy an explicit owner decision.
- `docs/testing/coverage-baseline.md:71-74,80-83,157-170` is a dated 2026-08-05/06 measurement but uses broad present-tense wording that says Track P is the only real-backend suite and no Track P scenario loads a plugin. Current TUI tests exercise a real backend (`packages/hya-tui-ts/test/real-backend.test.ts:7-8,47-69`, `real-backend-agents.test.ts:12-25`, `pty-smoke.test.ts:10-11,76-101`, `workflow-pty.test.ts:9-10,193-194`) and current P18 exercises plugin resources (`crates/hya-e2e/tests/p18_custom_slash_resources.rs:27-35,583-598,1420-1470`). Preserve the dated measurements; label those statements as historical snapshot scope or move current interpretation to the testing index.
- `docs/testing/coverage-baseline.md:71-73` links `.trellis/tasks/08-05-land-swarm-branch-to-main/findings.md`, but the retained file is under `.trellis/tasks/archive/2026-08/08-05-land-swarm-branch-to-main/findings.md`. This is a broken live reference even though the general docs link scan excluded Trellis task paths.
- `docs/testing/agent-matrix.md:116-140` contains stale flake anchors: the referenced `pty-smoke.test.ts:589` root-draft wording moved to `:1183-1223`, the sidecar test moved to `crates/hya-core/tests/subagent.rs:4098`, and the OAuth callback race starts at `crates/hya-app/src/oauth/callback.rs:151-159`. Replace line-number/string evidence with stable symbols and latest observed status.
- `docs/testing/README.md:27-34` omits CI's `--jobs 1` resource serialization on the workspace test step (`.github/workflows/ci.yml:87-90`). Either mirror the exact gate command or explain that local runs may omit the CI-only cap.
- `docs/testing/agent-matrix.md` has no provider/auth resilience scenarios despite focused Track I coverage in `crates/hya-provider/tests/conformance.rs:338-631`, `http_headers.rs:36-121,295-633,912-1217`, `multiprovider.rs:42-153`, and `crates/hya-core/tests/model_fallback.rs:228-478`. Add an index-only provider matrix row or state that provider coverage remains deliberately outside Track P/T.
- `docs/testing/agent-matrix.md:93-105` registers only T3.1–T3.4, while `workflow-presentation.test.ts`, `workflow-sidebar.test.ts`, and `workflow-pty.test.ts` provide current Workflow TUI coverage. Decide whether to add T3 rows or keep them non-matrix package smoke; do not gate the PTY suite without accepting its known flake risk (`.github/workflows/ci.yml:54-63`).
- `.github/workflows/release.yml:92-116` builds/packages only `hya`, `hya-backend`, and `hya-ts`; it does not include `hya-updater`. The self-update docs must state that the standard tarball does not ship `hya-updater`, unless release scope changes. Do not imply release support from source-only updater examples.

### Additional TUI command and invocation drift

- `docs/cli.md:60-61` gives `hya [PROJECT] [OPTIONS]`, but observed `hya --help` and `crates/hya-ts/src/lib.rs:16-70` expose `Usage: hya [OPTIONS] [PROJECT] [COMMAND]`, including forwarded `bundle`, `oauth`, `login`, and `auth` commands. Align the synopsis with actual help.
- `docs/cli.md:162-180` says the backend command catalog has seven built-ins and omits `/workflow`; `crates/hya-server/src/compat/command_catalog.rs:26-85` has eight non-expandable built-ins including `/workflow`. Add the row and retain the existing Workflow section at `:233-258`.
- `docs/tui-keybindings.md:347-358,536-543` lists `/models` only as `/mo` and `/variants` without `/model`/`/think`. Local registration provides `model.list` aliases `mo` and `model` (`packages/hya-tui-ts/src/upstream/app.tsx:562-569`) and `variant.list` alias `think` (`:645-650`); local exact-slash dispatch precedes backend command fallback (`packages/hya-tui-ts/src/upstream/component/prompt/index.tsx:961-969,1045-1068`). Document aliases and precedence.
- `docs/tui-keybindings.md:126,263,316` presents `session.queued_prompts` / `<leader>q` as an active command, but source search finds only the key definition/map (`packages/hya-tui-ts/src/upstream/config/keybind.ts:95,291`) and no handler in app command registration or session route (`packages/hya-tui-ts/src/upstream/app.tsx:78-116`; `routes/session/index.tsx:121-166`). Decide whether to remove active-command prose while retaining the accepted unwired definition or restore the handler in a separate product task.
- `docs/getting-started.md:17-18,77-81` and `packages/hya-tui-ts/README.md:52-57` invoke bare `hya` after building only workspace/selected binaries. In an uninstalled checkout, `target/debug` is not necessarily on `PATH`, and `crates/hya/src/main.rs:12-24` requires adjacent `hya-ts`; use `./target/debug/hya .` for checkout instructions and reserve bare `hya` for installed layouts.
- `packages/hya-tui-ts/README.md:36-37` claims a monorepo-root Bun workspace install, but the repository has no root `package.json`, `bun.lock`, or `bunfig.toml`; CI/release install from the package directory (`.github/workflows/ci.yml:35-40`, `.github/workflows/release.yml:117-125`). Remove the root-workspace claim.
- `packages/hya-tui-ts/README.md:169-185` says ship-time packaging copies “this package,” but release assembly copies a prepared runtime subset (`.github/workflows/release.yml:117-126`) and does not include package README/tests/scripts/NOTICE. Rewrite to say prepared runtime subset, and separately track the release `NOTICE` omission as a packaging defect (UPSTREAM requires NOTICE at `packages/hya-tui-ts/UPSTREAM.md:32-33`; installer copies it at `install.sh:208-211`, release does not).
- `docs/adr/0005-drop-legacy-tui-surface.md:8-9` says resume validates before navigation/stays on the current route on failure. Current TUI navigates immediately for `--session` (`packages/hya-tui-ts/src/upstream/app.tsx:467-473`), then loads and returns Home with a toast on missing/error (`routes/session/index.tsx:369-391`). Align the accepted ADR's current behavior or clearly mark the sentence historical.

### Historical records and intentionally preserved evidence

- `docs/superpowers/plans/2026-06-21-ratatui-tui-redesign.md` and `docs/superpowers/plans/2026-06-29-install-script.md` are historical planning evidence. Do not rewrite their old crate/package names or measurements in this documentation-sync task; only update a current index if it presents either plan as current guidance.
### Tracked AGENTS.md (separate dotfile workflow)

- `AGENTS.md:67-92` is a current component map but omits Workflow ownership from `hya-app`, `hya-bundle`, `hya-core`, `hya-server`, and `hya-tool`; current source ownership is `crates/hya-app/src/workflow_control.rs`, `crates/hya-bundle/src/lib.rs`, `crates/hya-core/src/workflow/`, `crates/hya-server/src/workflow*.rs`, and `crates/hya-tool/src/workflow_plane.rs`.
- `AGENTS.md:79` names only OpenAI-compatible, Anthropic, Google, dev, and fake providers; current configured provider kinds also include OpenAI Responses, OpenAI Codex, and Grok Build (`crates/hya-backend/src/main.rs:1080-1310`).
- `AGENTS.md:76-77,82` describes AgentBundle-only packaging, omits Workflow routes/control, and does not show the current `/sessions/:id/workflow` native and Compat workflow surfaces (`crates/hya-server/src/lib.rs:49-75`; `crates/hya-server/src/compat/session.rs:79-92`).
- `AGENTS.md:89` repeats the incorrect claim that the Rust `hya-plugin-compat` crate pins package versions; pins live in `crates/hya-plugin-compat/adapter/package.json:10-15`.
- `AGENTS.md:91` lists only `sync-compat`, `migrate`, `startup-bench`, and `matrix-check`; `crates/xtask/src/main.rs:6-15,30-40` also dispatches `package-bundle` and `release-rehearsal`.
- Any AGENTS.md correction must use the `agent-dotfile`/`writing-for-agents` workflow, preserve the managed Trellis block, and remain a separately owned documentation batch; do not edit the file directly in this task's planning pass.

***
- `docs/changes/CHANGELOG_*.md`, dated coverage tables, and archived Trellis task files remain immutable. Current docs may link or summarize them, but must not rewrite their historical claims.
## Audited current references with no confirmed stale claim

- `docs/cli.md`, `docs/configuration.md`, `docs/workflows.md` (except the model-id wording above), `docs/architecture/server-client.md`, `docs/architecture/storage.md`, `docs/architecture/tui-keybindings.md`, and current `.autors/hya/wiki/pages/architecture/{workflow-composition,provider-resilience}.md` matched the inspected CLI, route, workflow, provider, and TUI contracts. They should be used as source patterns for later edits, not duplicated wholesale.
- `docs/architecture/providers.md` was re-read against the current provider router, HTTP retry/failover boundaries, Workflow route terminology, and all six configured `ProviderKind` values. No confirmed stale claim remains; keep its retry bounds and provider-kind inventory as the source-backed pattern for related pages.

## Independent inventory reconciliation

The independent read-only audit is merged from `research/independent-inventory.md`. It inspected 681 tracked Markdown files, excluded 342 historical/vendor/managed/fixture files, classified 339 remaining files, and performed a local relative-link scan across 92 high-priority current documents. The audit scope covered root contributor/index documents (`AGENTS.md`, `README.md`, `CHANGELOG.md`, `CONTEXT.md`, `DESIGN.md`, `CLAUDE.md`), `.trellis/spec/` and workflow guidance, current `docs/`, `.autors/hya/wiki/`, package/crate READMEs, testing indexes, release metadata, and source/test/config evidence. The merged inventory found no missing relative Markdown target in that 92-document scan.

The detailed sections above already contain the independent report's root, Workflow, tool, Compat, TUI, testing, package, CLI, and operational findings. The following entries record additional report findings and explicit boundaries that were not yet represented there.

### Additional current-contract drift

- **Retained Rust TUI wording in active frontend specs (P0):** `.trellis/spec/frontend/directory-structure.md:18-19` calls `crates/hya-tui` and `crates/hya-tui-lib` retained compatibility crates; `.trellis/spec/frontend/component-guidelines.md:53` prohibits adding behavior there; `.trellis/spec/frontend/quality-guidelines.md:32,61` repeats the retained-renderer boundary. Those crates do not exist (`docs/adr/0010-remove-retained-rust-tui.md:3-14` records removal), and the shipped interactive frontend is only `packages/hya-tui-ts` (`AGENTS.md:105-107`; `docs/architecture/tui.md:439-446`). Replace the obsolete wording with the clean TypeScript-only boundary while preserving the existing `src/hya`/`src/upstream` ownership guidance.
- **Release verification contract (P0):** `.trellis/spec/backend/quality-guidelines.md:478-486` names three binaries and the prepared TUI but omits `lib/hya/compat-adapter` and `examples/hya-argus-example.hyabundle`, and it says to run `actionlint` without the executable rehearsal command or pinned tool versions. `.github/workflows/release.yml:108-129,161-188,244-248` packages and smokes both payloads; `crates/xtask/src/release_rehearsal.rs:18-26,93-119` pins actionlint 1.7.12/Bun 1.3.14 and runs the no-publish rehearsal. Expand the spec with the exact command, payload assertions, and the fact that CI does not currently run actionlint independently.
- **Compat adapter resolution precedence (P1):** `docs/configuration.md:706,1073-1075` describes only the workspace adapter path. `crates/hya-app/src/plugins.rs:80-112` resolves `HYA_COMPAT_ADAPTER_DIR`, then executable-adjacent `../lib/hya/compat-adapter`, then the workspace checkout. Document this order and the Bun override without changing runtime behavior.
- **TUI test inventory in development docs (P1):** `packages/hya-tui-ts/test/README.md:3,36-50,110-120` says eleven Bun test files, and `docs/development.md:68-83` omits `workflow-presentation.test.ts`, `workflow-sidebar.test.ts`, and `workflow-pty.test.ts`. The tracked package has 14 test files. Update counts and classify the three Workflow suites with their backend/PTY requirements.
- **Testing index tier range (P1):** `docs/testing/README.md:19` calls `agent-matrix.md` a Tier 0-2 inventory, while `crates/hya-e2e/matrix.toml:291-320` and `docs/testing/agent-matrix.md:93-105` register T3.1-T3.4. Use the actual T0-T3 range.
- **Canonical bundle authoring command (P1):** `docs/agent-bundle-authoring.md:60-70,382-386` prescribes raw `7z a` commands. `crates/xtask/src/package_bundle.rs:1-38` is the deterministic canonical writer, invoked by `scripts/package-argus-example.sh:13-17`; `tests/install_script.sh:28-33` rejects ad-hoc 7z creation in the helper. Make `cargo run -p xtask -- package-bundle <source-dir> <output.hyabundle>` primary; if raw 7z remains, label it a noncanonical manual alternative.
- **Backend spec index status (P1):** `.trellis/spec/backend/index.md:9,15-23` still says to fill every guide and marks database, error handling, quality, and logging as `To fill`, although database/error/quality contain substantive current contracts. Correct statuses and remove generic bootstrap wording only where the guide is populated; do not invent contracts for genuinely empty guides.
- **Release `NOTICE` payload decision (P2):** `packages/hya-tui-ts/UPSTREAM.md:31-33` tells packaged readers to see `NOTICE`, but `.github/workflows/release.yml:117-126` and `crates/xtask/src/release_rehearsal.rs:793-797` copy only `LICENSE` and `UPSTREAM.md`; `install.sh:208-211,261-271` does copy and verify `NOTICE`. This is an adjacent packaging decision: preferably include `NOTICE` in release/rehearsal, or remove the release-payload reference. Do not silently solve it as prose-only work.
- **Source usage-string command alias (P2):** `crates/xtask/src/main.rs:39`, `crates/xtask/src/package_bundle.rs:11`, and `crates/xtask/src/release_rehearsal.rs:125-126` say `cargo xtask`, but the workspace has no alias; `docs/development.md:103-110` correctly uses `cargo run -p xtask -- ...`. Keep documentation on the working invocation and record the source-string mismatch as an adjacent code follow-up, not as a documentation workaround.

### Stale links, paths, and anchors

- `docs/cli.md:336` links to `#db-empty-string-semantics`, but the heading is `` `--db` empty-string semantics `` at `docs/cli.md:32`; use `#--db-empty-string-semantics` or add a stable explicit anchor.
- `.trellis/spec/backend/workflow-control.md:107` points to `.trellis/tasks/08-28-durable-workflow-control/implement.md`; the retained file is `.trellis/tasks/archive/2026-08/08-28-durable-workflow-control/implement.md`.
- `docs/testing/coverage-baseline.md:72` and `.github/workflows/ci.yml:67` point to `.trellis/tasks/08-05-land-swarm-branch-to-main/findings.md`; the retained file is `.trellis/tasks/archive/2026-08/08-05-land-swarm-branch-to-main/findings.md`. This is a current-reference repair; do not edit the archived file.
- `Cargo.toml:2` references `.trellis/tasks/06-20-agent-spec/{design.md,implement.md}`; those artifacts are under `.trellis/tasks/archive/2026-06/06-20-agent-spec/`.
- `.trellis/workflow.md:708-709` references nonexistent `.trellis/spec/cli/backend/workflow-state-contract.md` and `.trellis/scripts/inject-workflow-state.py`; current implementations are `.codex/hooks/inject-workflow-state.py` and `.omp/extensions/trellis/index.ts`. Replace with real paths or state platform-specific ownership.
- `.trellis/spec/guides/code-reuse-thinking-guide.md:217-223` names upstream Trellis-template paths (`packages/cli/src/templates/trellis/scripts/` and `.trellis/scripts/packages/cli/...`) that are not hya paths. Mark that section template-only or remove it from project-local instructions.
- `.trellis/spec/backend/task-tool.md:302-304` says `tests/support/` without a crate root; the referenced helper appears under `crates/hya-app/tests/support/`. Make the path explicit.

### Boundaries and no-edit evidence

- Keep `docs/changes/CHANGELOG_*.md`, dated coverage tables, archived Trellis tasks, historical superpower plans, workspace journals, `.argus_subagents/**`, vendor/generated/node_modules/target, managed agent/skill/platform mirrors, and prompt/fixture sources immutable or excluded as classified in the independent report. Current docs may link or summarize historical evidence but must not rewrite its old claims.
- Do not regenerate dated coverage numbers without deliberate scope approval. Do not alter source usage strings or release `NOTICE` behavior in a documentation-only batch. Do not rewrite accepted ADR rationale except to repair a current path/behavior sentence or explicitly mark it historical.
- `AGENTS.md` changes are a separate tracked-agent-dotfile batch and MUST use the `agent-dotfile` plus `writing-for-agents` workflow; preserve the managed Trellis block and verify install/sync. They are not to be made directly during this planning pass.
- The independent report's current no-edit set remains authoritative: current Workflow routing prose in `docs/workflows.md` and the architecture wiki, `.trellis/spec/backend/workflow-control.md` except its stale path, `.trellis/spec/frontend/workflow-presentation.md`, P19/process-E2E references, the 28-tool matrix table, dated coverage tables, current TUI ownership/ADR removal records, `docs/cli.md` except the anchor/synopsis/catalog findings, package READMEs whose contracts match source, and superseded CI snippet marked as such.

### Verification inventory

Use the following targeted checks after implementation; they are planning evidence, not run in this planning pass:

```sh
cargo test -p hya --test version_metadata
cargo test -p hya-bundle --test docs_example
cargo run -p xtask -- matrix-check && cargo test -p hya-e2e --test p19_workflow_model_routing -- --test-threads=1
cd packages/hya-tui-ts && bun run typecheck && bun test test/workflow-presentation.test.ts test/workflow-sidebar.test.ts
cargo build -p hya-backend --bin hya-backend && cargo build -p hya-ts --bin hya-ts && cd packages/hya-tui-ts && bun test test/workflow-pty.test.ts
bash -n install.sh scripts/package-argus-example.sh tests/install_script.sh && bash tests/install_script.sh
cargo test -p xtask --test release_rehearsal
cargo run -p xtask -- release-rehearsal --workflow .github/workflows/release.yml --version 0.36.7 --target x86_64-unknown-linux-gnu --no-publish
actionlint .github/workflows/ci.yml .github/workflows/release.yml
```

The full rehearsal requires `actionlint` 1.7.12 and Bun 1.3.14 on `PATH`. Also run a repository-relative Markdown-link/anchor scan, a version consistency scan for `0.36.7`, a command/help comparison for documented invocations, and a code-span path scan. No build, test, formatter, or file-writing command was run by the independent inventory worker.
***
