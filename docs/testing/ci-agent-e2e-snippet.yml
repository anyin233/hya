# SUPERSEDED — this fragment is no longer needed.
#
# Track P (process agent E2E) and Track T are now wired directly into
# .github/workflows/ci.yml as enforced gate steps:
#
#   - name: test
#     run: cargo test --workspace --jobs 1 --exclude hya-e2e
#   - name: build agent e2e binaries
#     run: cargo build --locked -p hya-backend --bin hya-backend
#   - name: agent process e2e (Track P)
#     run: cargo test -p hya-e2e -- --test-threads=1
#
# Every gate step there carries `if: ${{ !cancelled() }}` so that one failing
# step reports on its own instead of skipping the rest of the gate.
#
# Track T's enforced set is named explicitly (real-backend.test.ts,
# task-presentation.test.ts, real-backend-agents.test.ts). The remainder of the
# bun suite, PTY smoke included, runs in a non-gating step.
#
# This file is kept only so existing links do not break.
# Docs: docs/testing/README.md, docs/testing/agent-matrix.md, docs/testing/process-e2e.md
